services:
  alice:
    build: .
    container_name: agora-alice
    hostname: alice
    environment:
      AGORA_USERNAME: alice
    ports:
      - "8081:8080"   # browser -> http://localhost:8081
    networks: [p2pnet]

networks:
  p2pnet:
    driver: bridge
